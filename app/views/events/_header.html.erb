<colgroup><col/><col/><col/><col/><col/><col/><col class="actions-col"></colgroup>
    <tr>
      <th><%= t('activerecord.attributes.event.client_id') %></th>
      <th><%= sortable_field(t('activerecord.attributes.event.code'), :code) %></th>
      <th><%= sortable_field(t('activerecord.attributes.event.name'), :name) %></th>
      <th><%= sortable_field(t('activerecord.attributes.event.start_at'), :start_at) %></th>
      <th><%= sortable_field(t('activerecord.attributes.event.end_at'), :end_at) %></th>
      <th><%= sortable_field(t('activerecord.attributes.event.address'), :address) %></th>
      <th><%= t('activerecord.attributes.event.resources') %></th>
      <th><%= t('events.users_assigned')%></th>
      <th><%= t('general.actions') %></th>
    </tr>
