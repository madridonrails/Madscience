    <colgroup><col/><col/><col/><col/><col class="actions-col"></colgroup>
    <tr>
      <th><%= sortable_field(t('activerecord.attributes.client.name'), :name) %></th>
      <th><%= sortable_field(t('activerecord.attributes.client.cif'), :cif) %></th>
      <th><%= sortable_field( t('activerecord.attributes.client.address'), :address) %></th>
      <th><%= sortable_field(t('activerecord.attributes.client.mail'), :mail) %></th>
      <th><%= t('activerecord.attributes.client.phone') %></th>
      <th><%= t('general.actions') %></th>
    </tr>
