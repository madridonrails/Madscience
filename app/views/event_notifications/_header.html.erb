  <!--%= link_to(@event.name, :controller => 'events', :action => 'send_resume', :id => @event, :host => @host) %-->
  <!--%= link_to (@event.name, event_path(@event), :host => @host) %-->
<%= @event.code %>
