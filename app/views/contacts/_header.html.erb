  <colgroup><col/><col/><col/><col class="actions-col"></colgroup>
    <tr>
      <th><%= sortable_field(t('activerecord.attributes.contact.name'), :name) %></th>
      <th><%= sortable_field(t('activerecord.attributes.contact.phone'), :phone) %></th>
      <th><%= sortable_field(t('activerecord.attributes.contact.email'), :email) %></th>
      <th><%= t('general.actions') %></th>
    </tr>
