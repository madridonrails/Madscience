    <colgroup><col/><col/><col/><col class="actions-col"></colgroup>
    <tr>
      <th><%= sortable_field(t('activerecord.attributes.user.login'), :login) %></th>
      <th><%= sortable_field(t('activerecord.attributes.user.email'), :email) %></th>
      <th><%= t('activerecord.attributes.user.is_admin') %></th>
      <th><%= t('general.actions') %></th>
    </tr>
