<%= t'assignment_mailer.pendant.body', :url => link_to(t('general.cancel'), :controller => 'assignments', :action => 'cancel', :event_id => @assignment.event.id, :id => @assignment.id, :host => @host) %>
